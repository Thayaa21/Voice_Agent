# Chetan — Agent Intelligence service (port 9001)
fastapi==0.111.0
uvicorn==0.30.0
httpx==0.27.0
python-dotenv==1.0.0
python-dateutil==2.9.0

# Conversation state machine + tools
langgraph>=0.2.0
langchain-core>=0.3.0

# LLM is optional and called over HTTP via httpx:
#   - OpenAI: set OPENAI_API_KEY  (uses the /v1/chat/completions REST API)
#   - Ollama: set OLLAMA_URL (e.g. http://localhost:11434) and OLLAMA_MODEL
# No extra SDK is required. If neither is set, the agent runs in a
# deterministic fallback mode (keyword routing + sentence trimming).
